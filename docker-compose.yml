version: '3'
services:
  p1qilinfront:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: p1qilinfront
    networks:
    - p1devnet
    ports:
    - "8080:8080"
    restart: unless-stopped

networks:
  p1devnet:
    external: true
